{
  "input_schema": {
    "type": "object",
    "title": "RecordLearningInput",
    "required": [
      "insight",
      "applies_to",
      "confidence",
      "source_result_id",
      "action"
    ],
    "properties": {
      "action": {
        "type": "string",
        "title": "Action"
      },
      "insight": {
        "type": "string",
        "title": "Insight"
      },
      "applies_to": {
        "type": "string",
        "title": "Applies To"
      },
      "confidence": {
        "type": "string",
        "title": "Confidence"
      },
      "source_result_id": {
        "type": "string",
        "title": "Source Result Id"
      },
      "linked_existing_learning_ids": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "title": "Linked Existing Learning Ids",
        "default": []
      }
    }
  },
  "name": "record_learning",
  "output_schema": {
    "type": "object",
    "title": "RecordLearningResult",
    "required": [
      "action",
      "learning_ids",
      "evidence_count"
    ],
    "properties": {
      "action": {
        "type": "string",
        "title": "Action"
      },
      "learning_ids": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "title": "Learning Ids"
      },
      "evidence_count": {
        "type": "integer",
        "title": "Evidence Count"
      }
    }
  },
  "permissions": {
    "function_id": "019f036c-b44b-7392-9119-1d3d5eb30225",
    "function_name": "record_learning",
    "grants": [
      {
        "resource_name": "learnings",
        "resource_type": "datastore_table",
        "permission_ids": [
          "datastore.record.read",
          "datastore.record.write",
          "datastore.table.read"
        ]
      },
      {
        "resource_name": "results",
        "resource_type": "datastore_table",
        "permission_ids": [
          "datastore.record.read",
          "datastore.table.read"
        ]
      }
    ]
  },
  "type": "API",
  "code": "#input_type_name: RecordLearningInput\n#output_type_name: RecordLearningResult\n#function_name: record_learning\n\nfrom datetime import datetime, timezone\nfrom pydantic import BaseModel\nfrom lemma_sdk import FunctionContext, Pod\n\nclass RecordLearningInput(BaseModel):\n    insight: str\n    applies_to: str\n    confidence: str\n    source_result_id: str\n    linked_existing_learning_ids: list[str] = []\n    action: str  # \"created\" | \"strengthened\"\n\nclass RecordLearningResult(BaseModel):\n    action: str\n    learning_ids: list[str]\n    evidence_count: int\n\nasync def record_learning(ctx: FunctionContext, data: RecordLearningInput) -> RecordLearningResult:\n    pod = Pod.from_env()\n    now = datetime.now(timezone.utc).isoformat()\n    learning_ids: list[str] = []\n    total_count = 0\n    if data.action == \"strengthened\" and data.linked_existing_learning_ids:\n        for lid in data.linked_existing_learning_ids:\n            row = pod.table(\"learnings\").get(lid)\n            new_count = int(row.get(\"evidence_count\", 1)) + 1\n            sources = list(row.get(\"source_result_ids\", []) or [])\n            sources.append(data.source_result_id)\n            pod.table(\"learnings\").update(lid, {\n                \"evidence_count\": new_count,\n                \"source_result_ids\": sources,\n                \"last_referenced_at\": now,\n            })\n            learning_ids.append(lid)\n            total_count += new_count\n        return RecordLearningResult(action=\"strengthened\", learning_ids=learning_ids, evidence_count=total_count)\n    row = pod.table(\"learnings\").create({\n        \"insight\": data.insight,\n        \"applies_to\": data.applies_to,\n        \"evidence_count\": 1,\n        \"source_result_ids\": [data.source_result_id],\n        \"last_referenced_at\": now,\n    })\n    return RecordLearningResult(action=\"created\", learning_ids=[str(row[\"id\"])], evidence_count=1)\n",
  "code_hash": "3257d78bedc0e94a3a580ff7c36d922687e2e15cfcb9c849ca2c817c33aed35d",
  "code_path": "record_learning.py",
  "config": null,
  "config_schema": null,
  "description": "Create a new learning, or strengthen existing ones, from a fresh result. Idempotent on insight text.",
  "python_packages": [],
  "visibility": "POD"
}