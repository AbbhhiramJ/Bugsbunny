# assets/

The Lemma pod also stores `logo.svg` at
`/me/buggs-bunny/assets/logo.svg`. The copy in this folder is identical and
re-uploadable via `lemma files upload assets/logo.svg
/me/buggs-bunny/assets/logo.svg`.
